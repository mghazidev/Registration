export type CountryCode = {
  code: string;
  country: string;
  flag: string;
  spellingCode: string;
  key: string;
};

export type PhoneInputValue = {
  countryCode: string;
  phoneNumber: string;
};
